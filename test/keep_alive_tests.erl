%%%-------------------------------------------------------------------
%% @doc keep_alive tests.
%% @end
%%%-------------------------------------------------------------------

-module(keep_alive_tests).
-include_lib("eunit/include/eunit.hrl").

%% Test cases

keep_alive_start_test() ->
  % {ok, _Pid} = keep_alive:start_link(),
  % % Assert that the process is alive
  % ?assert(is_pid(_Pid)).
  ?assert(false).
