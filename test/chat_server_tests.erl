%%%-------------------------------------------------------------------
%% @doc chat_server_tests.erl
%% Tests for the chat server
%% @end
%%%-------------------------------------------------------------------

-module(chat_server_tests).

-include_lib("eunit/include/eunit.hrl").

%% Test WebSocket Connection
% server_up_test() ->
%     %% Start the application
%     Bla = application:start(chat_server),
%
%     %% Verify the server is running
%     ?assertEqual(ok, Bla),
%
%     %% Clean up
%     application:stop(chat_server).
