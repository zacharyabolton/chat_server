services:

  chat_server_development:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - ENV=development
    develop:
      watch:
        - action: rebuild
          path: ./src
          target: /app/src
